"""Протокол внешнего суммаризатора рассуждений для слоя контекста.

Слой контекста не умеет (и не должен) звать модель сам: это нарушило бы его
изоляцию от Config/ModelClient. Вместо этого ContextManager принимает извне
объект, реализующий ReasoningSummarizer, и обращается к нему, когда история
превышает токеновый порог. Конкретная реализация на базе ModelClient живёт в
верхнем слое harness и внедряется через конструктор (инверсия зависимостей).
"""

from __future__ import annotations

from typing import Protocol

from .history import HistoryEntry


class ReasoningSummarizer(Protocol):
    """Сворачивает старые ходы истории в компактную накопительную сводку.

    Реализация не мутирует entries и возвращает новую сводку. Пустая строка
    означает «не сворачивать»; любое исключение реализации вызывающая сторона
    трактует так же — состояние контекста при этом не меняется (детерминированный
    fallback).
    """

    def summarize(self, entries: list[HistoryEntry], previous_summary: str) -> str:
        """Возвращаем обновлённую сводку по ходам entries и предыдущей сводке."""
