[project]
name = "madharness-mini"
version = "0.2.0"
description = "Minimal dependency-free coding agent harness for the madharness course."
requires-python = ">=3.13"
license = "PolyForm-Noncommercial-1.0.0"
dependencies = []

[project.scripts]
madharness-mini = "madharness_mini.__main__:main"
context-heatmap = "context_heatmap.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
